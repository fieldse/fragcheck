// Command linux-vuln-auditor audits the local Linux host for exposure to a
// curated set of recent kernel privilege-escalation CVEs and prints a
// remediation report.
//
// This is the phase-1 skeleton: packages and core types are in place but the
// collect/detect/report pipeline is not yet wired. See docs/SPEC.md.
package main

import "fmt"

func main() {
	fmt.Println("linux-vuln-auditor: scaffolding in place; pipeline not yet wired")
}
